# Deployment

OpenMacaw does not currently publish production deployment infrastructure.

The initial public import intentionally excludes private Terraform state
configuration, cloud account identifiers, environment-specific deployment
values, and internal deployment workflows. Public deployment documentation will
be added after the self-hosting path is defined with sanitized examples.

For now, use the subsystem local-development commands documented in the root
README and the open-source readiness scope.
