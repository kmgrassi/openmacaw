# Local worker

live_worker_entrypoint.sh starts an SSH daemon suitable for local worker-host testing.

Example: mount an authorized key at /run/symphony/ssh/authorized_key.pub and run this entrypoint.
